<?xml version="1.0" encoding="UTF-8"?>
<configuration>
<!-- [로그 레벨 수준 ]
		1) TRACE 	- 	자세히 모두 출력, 특히 루프문의 내부 흐름을 체크할때 예)for문에서 3번째 i가 현재값이 무엇인지..
							마치 현미경처럼 애플리케이션 내부의 정보를 알고싶을 경우.. (현장에서는 거의 쓸일이 없음)
		
		2) DEBUG	-	개발단계에서 가장많이 사용
							로직의 흐름 확인 용, 로직의 주요 분기, 실행부가 메서드에 진입/종료
							
		3) INFO		-	개발단계 보다는 운영쪽에서 많이 사용
							서버의 시작/종료 여부 등을 출력, 주요 이벤트 발생을 알기 위해..
							개발에 사용할 경우 사용자가 로그인을 성공했는지, 주문을 완료했는지(큼지 막한 이벤트..)
							
		4) WARN	-	당장 장애는 아니지만, 곧 문제가 될 수 있는 소지가 있는 경우 (신호등의 노란불 정도?)
							
		5) ERROR 	-	예외가 발생한 경우, 요청처리 실패 시 ..( 빨간불 )
		
		6) FATAL		-	서비스 지속 불가능, 서버 동작 불가 수준		

 -->
	<!-- 콘솔에 출력할 용도로만 설정, (파일도 가능) -->
	<appender name="CONSOLE" class ="ch.qos.logback.core.ConsoleAppender">
	<encoder>
		<!-- 개발자가 원하는 패턴을 명시 ex) 시간 - 내용 -->
		<pattern>
		<!-- %날짜 형식 %(새로운패턴지정)  - (왼쪽 정렬) 5(5칸 간격으로) level(log level 출력) [](표현용 다른 것도 가능)
				thread (이름) logger ( 로거 명 = 패키지 포함 경로 명) -(표현용) msg(개발자가 출력한 문자열) n(줄바꿈)
		-->
			%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{40} -%msg%n
		</pattern>
	</encoder>
	</appender>
	<!-- 어느 수준으로 로그를 사용할지 레벨을 등록 
			개발자가 로그 레벨을 선언하면 , 그 선언보다 높은 레벨까지 모두 출력됨
			사용 이유 sysout 으로 출력하면 개발이 끝난후 모두 주석 처리해야하지만
			아래의 레벨을 선언하면 그 이전 레벨은 기능 하지 않고 이후 레벨만이 동작
	-->
	<root level="INFO">
		<appender-ref ref="CONSOLE"/>
	</root>
	<!-- 아래처럼 패키지별로 로그레벨을 조정하면 더 구체적일 수록 로그에 적용 우선순위가 높아지므로 
			위에서 루트를 선언하여도 아래의 디버그가 우선실행
	 -->
	<logger name="com.ch.shop.controller" level="DEBUG"/>
	
	
</configuration>